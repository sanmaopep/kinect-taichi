#pragma once
#include"stadfx.h"
#include"feature.hpp"

using namespace std;

namespace taiji {

	// 基于Kmeans的算法


	// 单层曲线简化算法
	vector<Feature> simpleCurve(const vector<Feature>& seq) {
		// K结果，C为极值候选集合
		vector<Feature> K,C;

		// 遍历找极值

		return K;
	}
}